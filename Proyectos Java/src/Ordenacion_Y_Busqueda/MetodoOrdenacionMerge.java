package Ordenacion_Y_Busqueda;

public class MetodoOrdenacionMerge {
    public static void main(String[] args){

    }

    /*static int OrdMezcla(int v[], int l, int r){
        if(l==r){
            return 0;   
        }
        int mid=((l+r)/2);
    }*/

    /* ordMezcla(int v[], int l, int l)
     * if(l==r)){
     *      return 0;
     * }
     * int mid <- (l+r)/2;
     * OrdMezcla(v, l, mid)
     * OrdMezcla(v, mid+1, r)
     * Queve <Integer> L <- new LinkedList<Integer>();
     * Queve <Integer> R <- new LinkedList<Integer>();
     * for(i<-l to mid){
     *     l.offer(V[i]);
     * }
     * for(i <- mid+1 to r){
     *     R.offer(V[i]);
     * }
     * for(i <- l to r){
     *     if(l.size()==0){
     *         v[i] <- R.poll()
     *     }else{
     *         if()
     *          }
     * }
      */
}
